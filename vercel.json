{
  "framework": "angular",
  "buildCommand": "ng build",
  "outputDirectory": "dist/ginasio-xl-app/browser",
  "routes": [
    { "src": "/api/auth/(.*)", "dest": "/api/auth" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
